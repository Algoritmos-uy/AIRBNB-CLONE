# Video placeholder

Place your shared demo video here as `demo.mp4`.

Example structure:

```text
client/
  assets/
    video/
      demo.mp4
```

`router.js` references `/assets/video/demo.mp4` for all property detail pages.
